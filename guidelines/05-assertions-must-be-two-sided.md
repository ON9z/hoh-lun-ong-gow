# 断言必须双向夹逼：单向断言对「放松」方向失明

判据：我的断言在**放松**那个方向上也变红吗？

**实例**：断言写成「**误报数 == 0**」。而**放松阈值永远不会产生误报** ⇒ 把阈值放宽 10 倍，
断言**仍然全绿**，而检查已经形同虚设。**「已锁」当时是一句空话。**

**⇒ 做法**：上下界都要有**依据**（上界 = 最紧的零误报值；下界 = 「失效的指纹」），
并**实测两侧都会红**、默认值会绿。

**同族**：守卫要守**类别**不守**形状**（正则扫写法 ⇒ 换个拼写就失效）；
且**零消费者的检查 = 死代码**，它的注释会持续说谎。

